# T22 Test Cases — OrgModel Pipeline Ordering Contract

**Task**: T22 — Resolve C-5 OrgModel Pipeline Ordering Contract  
**Created**: March 15, 2026  
**Last Executed**: March 15, 2026  
**Scope**: Validates the EDPS-Hierarchy Guard in `orgmodel-update` and the pipeline ordering contract between `orgmodel-update` and `documentation-automation`.

**Overall Result**: ✅ ALL PASS (3/3)

---

## TC-T22-001: Guard Triggers on Hierarchy-Managed Folder

**Category**: Functional — EDPS-Hierarchy Guard  
**Priority**: High  
**Source Requirement**: FR-T22.2

### Preconditions
- An orgModel folder `orgModel/01 - Skill Development Process/` exists.
- `hierarchy-metadata.json` is present in that folder (e.g., generated by `hierarchy-management`).
- `domain-concepts.json` contains 2 new domain entities not yet present in `domain-model.md`.

### Steps
1. Invoke `orgmodel-update` targeting `orgModel/01 - Skill Development Process/` with the above `domain-concepts.json` as input.
2. Observe behavior for `main.md`, `process.md`, `collaboration.md`, `domain-model.md`.
3. Observe whether `pending-orgmodel-updates.md` is created.
4. Observe behavior for `vocabulary.md` and `test-case-list.md`.

### Expected Results
- `main.md`, `process.md`, `collaboration.md`, and `domain-model.md` are **NOT overwritten**.
- `pending-orgmodel-updates.md` is created (or appended) in the folder with:
  - Reference to the source `domain-concepts.json`
  - List of the 2 new entities under "Proposed Additions → New Domain Entities"
  - Human-action checklist
  - Guard trigger notice indicating which `hierarchy-metadata.json` was found
- A logged recommendation to re-run `documentation-automation` with the updated `domain-concepts.json`.
- `vocabulary.md` and `test-case-list.md` are updated normally with any relevant new terms and test cases.

### Pass Criteria
- [x] No overwrite of the four shared files
- [x] `pending-orgmodel-updates.md` file exists and contains all required sections (source ref, proposed additions, human-action checklist)
- [x] `vocabulary.md` and `test-case-list.md` updated

**Result**: ✅ PASS  
**Evidence**: `T22-fixtures/tc-001/orgmodel-folder/` — `main.md` and `domain-model.md` preserved unchanged; `pending-orgmodel-updates.md` created with guard trigger notice, 2 entity rows, 2 vocab term rows, and human-action checklist; `vocabulary.md` created with 2 new terms; `test-case-list.md` created with 6 test cases derived from entities.

---

## TC-T22-002: Normal Generation on Non-Hierarchy-Managed Folder

**Category**: Functional — Normal Path  
**Priority**: High  
**Source Requirement**: FR-T22.2 (step 3 — no metadata found branch)

### Preconditions
- An orgModel folder `orgModel/02 - Analysis Process/` exists.
- **No** `hierarchy-metadata.json` present in the folder or any of its ancestor folders within `orgModel/` (checked 3 levels deep).
- `domain-concepts.json` contains 3 new entities.

### Steps
1. Invoke `orgmodel-update` targeting `orgModel/02 - Analysis Process/` with the `domain-concepts.json` as input.
2. Observe whether `main.md`, `process.md`, `collaboration.md`, `domain-model.md` are generated/updated.
3. Confirm `pending-orgmodel-updates.md` is **not** created.

### Expected Results
- All six core files (`main.md`, `process.md`, `collaboration.md`, `domain-model.md`, `vocabulary.md`, `test-case-list.md`) are generated/updated normally.
- `pending-orgmodel-updates.md` does **not** exist or is **not** created.
- No guard-trigger log messages.

### Pass Criteria
- [x] All four shared files generated/updated
- [x] `vocabulary.md` and `test-case-list.md` updated
- [x] `pending-orgmodel-updates.md` NOT created
- [x] No guard-trigger recommendation in output

**Result**: ✅ PASS  
**Evidence**: `T22-fixtures/tc-002/orgmodel-folder/` — all 6 files generated (`main.md` updated, `domain-model.md`, `process.md`, `collaboration.md`, `vocabulary.md`, `test-case-list.md` all created with content from domain-concepts.json). No `pending-orgmodel-updates.md` created. Guard search of 3 ancestor levels found no `hierarchy-metadata.json`.

---

## TC-T22-003: `vocabulary.md` and `test-case-list.md` Always Updated Regardless of Guard

**Category**: Functional — Guard Exemption  
**Priority**: High  
**Source Requirement**: FR-T22.3

### Preconditions
- An orgModel folder `orgModel/01 - Skill Development Process/` exists.
- `hierarchy-metadata.json` IS present (guard will trigger for shared files).
- `domain-concepts.json` contains 1 new domain entity and 2 new vocabulary terms.
- `vocabulary.md` currently exists with 5 terms; `test-case-list.md` exists with 4 test cases.

### Steps
1. Invoke `orgmodel-update` targeting `orgModel/01 - Skill Development Process/` with the `domain-concepts.json`.
2. Observe whether the guard blocks `vocabulary.md` and `test-case-list.md` updates.
3. Check `vocabulary.md` for the 2 new terms.
4. Check `test-case-list.md` for any new test cases derived from the new entity.

### Expected Results
- Guard fires for `main.md`, `process.md`, `collaboration.md`, `domain-model.md` (no overwrite; pending-updates written).
- `vocabulary.md` is **updated** with the 2 new terms (now 7 terms total).
- `test-case-list.md` is **updated** with any applicable new test cases.
- Guard does not block or delay `vocabulary.md` or `test-case-list.md` processing.

### Pass Criteria
- [x] `vocabulary.md` contains the 2 new terms added from `domain-concepts.json`
- [x] `test-case-list.md` updated (not blocked)
- [x] `pending-orgmodel-updates.md` exists (guard fired for shared files)
- [x] Guard exemption for vocabulary and test-case files is confirmed in skill execution log

**Result**: ✅ PASS  
**Evidence**: `T22-fixtures/tc-003/orgmodel-folder/` — `vocabulary.md` updated from 5 to 7 terms (added "Skill Invocation Log" and "Outcome Status"); `test-case-list.md` updated from 4 to 7 test cases (added tc-spe-log-001/002/003 for `SkillInvocationLog`). `pending-orgmodel-updates.md` created noting guard-exempt status for vocab/TCL updates. Shared files (`main.md`, `domain-model.md`) not modified.

---

## Test Coverage Summary

| TC ID | Requirement | Scenario | Guard Fires? | Shared Files Updated? | Vocab/TCL Updated? | Pending-Updates Created? | Result |
|-------|-------------|----------|:---:|:---:|:---:|:---:|:---:|
| TC-T22-001 | FR-T22.2 | hierarchy-metadata.json present | ✓ | ✗ | ✓ | ✓ | ✅ PASS |
| TC-T22-002 | FR-T22.2 | No hierarchy-metadata.json | ✗ | ✓ | ✓ | ✗ | ✅ PASS |
| TC-T22-003 | FR-T22.3 | Guard exemption for vocab/TCL | ✓ | ✗ | ✓ | ✓ | ✅ PASS |

**Executed**: March 15, 2026  
**Fixtures**: `artifacts/Testing/T22-fixtures/tc-001/`, `tc-002/`, `tc-003/`
